services:
  app:
    build: .
    container_name: rl-analytics
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./Replay Data:/app/Replay Data
    restart: unless-stopped